
import React from 'react';
import { Link } from 'react-router';

const App = ({ children }) => (
  <div>
    <header>
    </header>
    <main>
      {children}
    </main>
  </div>
);

export default App;
